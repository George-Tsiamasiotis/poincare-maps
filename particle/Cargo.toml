[package]
name = "particle"
version = "0.1.0"
edition = "2024"
authors.workspace = true

[dependencies]
serde = { workspace = true }
thiserror = {workspace = true}
rsl-interpolation = {workspace = true}
config = {path = "../config"}
equilibrium = {path = "../equilibrium"}

[features]
default = []
lar = []
error-adaptive-step = []

[[bin]]
name = "integrate-particle"
