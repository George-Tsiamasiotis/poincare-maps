### crate "equilibrium"

Handles the reconstruction of equilibria from NetCDF files, providing interpolation methods for calculating 
all the relevant quantities, as well as extraction of the underlying data.
